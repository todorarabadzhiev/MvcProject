@model WildCampingWithMvc.Models.SiteCategory.AddSiteCategoryViewModel
@{
    ViewBag.Title = GlobalResources.TextEditSiteCategory;
}

<h2 class="text-center">@ViewBag.Title.</h2>

@using (Html.BeginForm("EditSiteCategory", "SiteCategory", FormMethod.Post, new { @class = "form-horizontal", role = "form", enctype = "multipart/form-data" }))
{
    Html.RenderPartial("_EditSiteCategory", Model);
    <div class="form-group">
        <div class="col-md-offset-4 col-md-4">
            <input type="submit" class="btn btn-block" value=@GlobalResources.TextEdit />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/category")
}